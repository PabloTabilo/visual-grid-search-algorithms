import {Square} from './Square.js';
import 'jest-canvas-mock';

describe("Test Square class using canvas", ()=>{
    let canvas = document.createElement("canvas");
    let size = 20;
    canvas.width = 1280;
    canvas.height = 720;
    let c = canvas.getContext('2d');
    test("Test me", ()=>{

    });
});